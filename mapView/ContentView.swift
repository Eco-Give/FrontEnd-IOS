import SwiftUI
import MapboxMaps

// ContentView
struct ContentView: View {
    var body: some View {
        //mapBoxView()
       //map()
        annotationView()
    }
}

// Preview
struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
